# Git
.git
.gitignore

# Python cache
__pycache__/
*.py[cod]
*.pyo

# Local persistent state — comes from the mounted volume, not the image.
# NOTE: soul.md is intentionally NOT ignored: the shipped copy is used to
# seed the data directory on first run.
data/
memory.json
output.txt
session_*.json
conversation.json

# Editors and OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
